let names = ["jivi", "manyu", "chirag","rishab", "tushar"]

// console.log(names.length);

console.log(names);

names.push("sujal") //Adds element at last
console.log(names);

console.log(names.pop()); //Removes and returns the element from the last